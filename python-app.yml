RUN pip install --no-cache-dir -r requirements.txt
stages:
  - build
  - test

build-code-job:
  stage: build
  script:
    - docker build -t my-docker-image .
    - docker run my-docker-image test.py

